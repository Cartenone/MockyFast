These are the ten YAML examples the README documented at commit 28834c2, the
first published version of the format. They are kept verbatim so a change to
the configuration format cannot quietly break a file someone copied from it.

Regenerate them with:

    git show 28834c2:README.md
